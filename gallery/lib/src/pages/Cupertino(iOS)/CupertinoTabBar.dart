import 'package:flutter/material.dart';

import '../../components/Content.dart';

class H_CupertinoTabBar extends StatelessWidget {
  H_CupertinoTabBar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Content(
      title: 'CupertinoTabBar',
      child: Text('CupertinoTabBar'),
    );
  }
}
