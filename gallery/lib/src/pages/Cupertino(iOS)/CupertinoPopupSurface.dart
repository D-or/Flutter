import 'package:flutter/material.dart';

import '../../components/Content.dart';

class H_CupertinoPopupSurface extends StatelessWidget {
  H_CupertinoPopupSurface({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Content(
      title: 'CupertinoPopupSurface',
      child: Text('CupertinoPopupSurface'),
    );
  }
}
