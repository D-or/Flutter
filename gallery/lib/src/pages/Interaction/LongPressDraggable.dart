import 'package:flutter/material.dart';

import '../../components/Content.dart';

class H_LongPressDraggable extends StatelessWidget {
  H_LongPressDraggable({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Content(
      title: 'LongPressDraggable',
      child: Text('LongPressDraggable'),
    );
  }
}
