import 'package:flutter/material.dart';

import '../../components/Content.dart';

class H_Dismissible extends StatelessWidget {
  H_Dismissible({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Content(
      title: 'Dismissible',
      child: Text('Dismissible'),
    );
  }
}
