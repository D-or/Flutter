import 'package:flutter/material.dart';

import '../../components/Content.dart';

class H_Navigator extends StatelessWidget {
  H_Navigator({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Content(
      title: 'Navigator',
      child: Text('Navigator'),
    );
  }
}
