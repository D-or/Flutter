import 'package:flutter/material.dart';

import '../../components/Content.dart';

class H_Flow extends StatelessWidget {
  H_Flow({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Content(
      title: 'Flow',
      // child: Flow(
      //   delegate: null,
      //   children: <Widget>[
      //     Text('Flow'),
      //   ],
      // ),
    );
  }
}
