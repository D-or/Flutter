import 'package:flutter/material.dart';

import '../../components/Content.dart';

class H_FractionalTranslation extends StatelessWidget {
  H_FractionalTranslation({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Content(
      title: 'FractionalTranslation',
      child: Text('FractionalTranslation'),
    );
  }
}
