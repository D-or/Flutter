import 'package:flutter/material.dart';

import '../../components/Content.dart';

class H_AnimatedModalBarrier extends StatelessWidget {
  H_AnimatedModalBarrier({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Content(
      title: 'AnimatedModalBarrier',
      child: Text('AnimatedModalBarrier'),
    );
  }
}
